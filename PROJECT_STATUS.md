# Project Status: AI QUANTUM OKX Auto-Trader

## Current Status
- **v2.1.4 Regime Engine Upgrade**: 시장 판별 로직을 3영역(Trend/Neutral/Range)으로 고도화하고 민감도 설정을 정상화함.
- **v2.1.3 Scanner Visualizer**: 스캐너 상단에 시장 성격(Trend/Neutral/Conservative)별 종목 수 시각화 지표 추가.

## v2.1.4 (2026-05-12)
- [System] **3영역 시장 판별 시스템 도입**: 단순 이분법(Trend/Range)에서 벗어나 ADX 기반의 3영역(강추세/전환기/박스권) 판별 로직을 도입하여 시장 성격 분석의 정밀도를 높임.
- [Logic] **민감도 논리 정상화**: '공격' 설정 시 더 낮은 ADX에서도 추세를 포착하고, '보수' 설정 시 더 엄격한 기준으로 추세를 판별하도록 로직을 재설계함.
- [UI/UX] **반응형 스캔 동기화**: 민감도 설정 변경 시 기존 스캔 결과를 즉시 초기화하여 사용자에게 실시간 계산 상태를 명확히 전달함.
- [UI/UX] **시장 성격 시각화 지표 추가**: 스캐너 화면 상단에 3색 원형 인디케이터(Red: Trend, Yellow: Neutral, Blue: Conservative)를 배치하여 현재 시장의 전반적인 추세 분포를 한눈에 파악 가능하도록 개선.
- [UI/UX] **프리미엄 디자인 적용**: Glassmorphism 스타일과 JetBrains Mono 폰트, 미세한 그림자 효과를 적용하여 가독성과 시각적 완성도를 높임.
- [UI/UX] **하이브리드 손익 설정 UI**: 슬라이더(빠른 조절)와 숫자 입력기(정밀 입력)를 동시에 배치하여 편의성을 극대화함. 숫자 입력기를 통해 **0.01% 단위의 초정밀 설정**이 가능해짐.
- [System] **설정 로드 신뢰성 보강**: `core/config.py` 최상단에 `load_dotenv(override=True)`를 강제 호출하여, 노트북 재부팅 및 앱 재실행 시 항상 `.env` 파일의 최신값이 최우선으로 로드되도록 보장함.

## v1.2.09 (2026-05-12)
- [UI/UX] **설정 정밀도 향상**: 대시보드 하단 설정 섹션의 손절(SL) 및 익절(TP) 슬라이더 조절 단위를 **0.25%**로 세분화함. 이제 1.25%, 1.75%와 같은 미세한 전략적 수치 조절이 가능함.

## v1.2.08 (2026-05-12)
- [Feature Removal] **서버중지 버튼 삭제**: UI 복잡성을 줄이고 시스템 안정성을 위해 상단 탭라인의 서버중지 버튼과 관련 종료 로직, 전용 CSS 스타일을 모두 제거함. 헤더 레이아웃을 이전의 4컬럼 구성(시간, 상태, 새로고침)으로 복구함.

## v1.2.07 (2026-05-12)
- [System] **서버중지 기능 표준 컴포넌트 복구**: HTML 링크 방식에서 발생할 수 있는 작동 불량 문제를 해결하기 위해 표준 `st.button`으로 기능을 복구함.
- [UI/UX] **궁극의 스타일링 강제 적용**: `div[data-testid="stButton"] button`을 타겟팅하는 강력한 CSS 셀렉터를 통해 **빨간색 바탕(#ef4444)**과 **흰색 텍스트**를 강제로 구현함. 컬럼 너비를 확장하여 버튼이 잘리지 않도록 하고 수직 정렬을 완벽하게 맞춤.

## v1.2.06 (2026-05-12)
- [UI/UX] **서버중지 버튼 초정밀 재구축**: 스트림릿 컴포넌트의 테마 간섭을 100% 배제하기 위해 순수 HTML과 인라인 CSS 스타일로 버튼을 재구축함. **선명한 빨간색 배경(#ef4444)**과 흰색 텍스트를 고정하고, `flex` 컨테이너와 픽셀 단위 패딩 조정을 통해 시간 표시 및 배지들과 **완벽하게 동일한 수평선상**에 놓이도록 보정함.

## v1.2.05 (2026-05-12)
- [UI/UX] **스타일링 강제 적용**: 스트림릿 내부의 `stButton` 셀렉터를 직접 타겟팅하여, 기본 테마에 의해 버튼 색상이 회색으로 덮여씌워지는 문제를 해결함. 이제 **선명한 빨간색 배경**과 **흰색 볼드 텍스트**가 어떤 상황에서도 유지되도록 강제 주입함.

## v1.2.04 (2026-05-12)
- [UI/UX] **서버중지 버튼 완전 재구축**: 스트림릿 기본 버튼의 스타일 제약을 극복하기 위해 커스텀 HTML(`<a>` 태그) 기반 버튼으로 전면 재구축함. 쿼리 파라미터 기반의 종료 로직을 도입하여, 이제 브라우저 환경에 관계없이 **선명한 빨간색 타원형 디자인**과 **완벽한 세로 정렬**이 보장됨.

## v1.2.03 (2026-05-12)
- [UI] **서버중지 버튼 디자인 정교화**: 버튼 색상을 선명한 빨간색(#ef4444)으로 고정하고 글자색을 흰색으로 변경하여 가시성을 극대화함. 또한 `LIVE CONNECTION` 및 `REFRESH` 버튼과 동일한 선상에 놓이도록 세로 정렬(Vertical Alignment)을 보정함.

## v1.2.02 (2026-05-12)
- [Feature] **서버 강제 중지 버튼 추가**: 상단 탭라인의 시간 표시와 상태 배지 사이에 빨간색 원형의 '서버중지' 버튼을 구현함. 클릭 시 `os._exit(0)`을 호출하여 터미널에서 실행 중인 Streamlit 서버를 즉시 안전하게 종료할 수 있음.

## v1.2.01 (2026-05-12)
- [UI] **폰트 두께 조정**: '일 평균 수익률' 수치 텍스트가 너무 강조되어 보인다는 피드백을 반영하여, 폰트 두께를 볼드(700)에서 보통(500)으로 하향 조정하여 세련된 느낌을 강화함.

## v1.2.00 (2026-05-12)
- [UI] **지표 폰트 크기 고정**: '일 평균 수익률' 지표 하단의 가동 기간 날짜 폰트 크기를 사용자의 요청에 따라 명확하게 **`14px`**로 고정하여 시인성을 확보함. 프로젝트 마일스톤 달성에 따라 버전을 `v1.2.00`으로 업데이트함.

## v1.1.99 (2026-05-12)
- [UI] **지표 날짜 색상 변경**: '일 평균 수익률' 지표 하단의 가동 기간(`05/10/2026~`) 텍스트 색상을 시스템의 성장과 수익을 상징하는 녹색(#22c55e)으로 변경하여 시각적 강조 효과를 부여함.

## v1.1.98 (2026-05-12)
- [UI] **지표 화살표 제거**: '일 평균 수익률' 지표 하단의 시작일(`05/10/2026~`) 부분에 자동으로 표시되던 윗방향 화살표를 제거하기 위해 해당 지표를 커스텀 HTML 카드로 전환함.

## v1.1.97 (2026-05-12)
- [UI] **날짜 표시 형식 변경**: '일 평균 수익률' 지표 하단의 시작일 안내 문구를 사용자의 요청에 따라 `Since MM/DD`에서 `MM/DD/YYYY~` 형식으로 더욱 명확하게 수정함.

## v1.1.96 (2026-05-12)
- [Metric] **일 평균 수익률 추가**: Git 로그의 최초 커밋 날짜를 추적하여 시스템 가동 기간을 산출하고, 이를 바탕으로 '일 평균 수익률' 지표를 누적 수익률과 승률 사이에 추가함.

## v1.1.95 (2026-05-12)
- [Fix] **폰트 크기 제한 우회**: 브라우저의 최소 폰트 크기 제한으로 인해 더 이상 작아지지 않는 문제를 해결하기 위해 `transform: scale(0.77)`을 적용하여 버튼 전체를 강제로 축소함. 동시에 높이와 너비도 추가 77% 수준으로 조정함.

## v1.1.94 (2026-05-12)
- [UI] **청산 버튼 폰트 추가 축소**: 버튼 내부의 텍스트가 여전히 크다는 피드백을 반영하여, 직전 버전 대비 77% 수준인 **`0.34rem`**으로 폰트 크기를 대폭 축소하여 정보 밀도를 극대화함.

## v1.1.93 (2026-05-12)
- [Fix] **버튼 가로 길이 강제 적용**: 스트림릿의 `use_container_width=True` 설정이 CSS의 가로 폭 조정을 무시하던 문제를 해결하기 위해, 해당 설정을 끄고 CSS에서 직접 가로 길이를 `45%`로 고정하여 실제 시각적 축소를 완료함.

## v1.1.92 (2026-05-12)
- [UI] **청산 버튼 가로 폭 최적화**: 버튼이 여전히 가로로 길다는 피드백을 반영하여, 직전 버전 대비 가로 길이를 66% 수준인 `45%`로 대폭 축소하여 매우 슬림한 디자인을 적용함.

## v1.1.91 (2026-05-12)
- [UI] **UI 콤팩트화 극대화**: 청산 버튼이 여전히 크다는 피드백을 반영하여, 이전 크기(v1.1.90) 대비 88% 수준(0.44rem, 19px, 68% width)으로 추가 축소하여 정보를 최대한 밀도 있게 표시함.

## v1.1.90 (2026-05-12)
- [UI] **청산 버튼 축소**: 화면 공간 활용도를 높이기 위해 '청산' 관련 버튼의 폰트 크기, 높이, 너비를 기존 대비 77% 수준(0.5rem, 22px, 77% width)으로 축소함.

## v1.1.89 (2026-05-12)
- [UI] **명령어 가독성 강화**: 설명 텍스트뿐만 아니라 실제 명령어 부분(`<code>` 태그)도 원본 대비 122%인 `0.8rem`으로 확대하고 색상을 밝게 조정하여 가독성을 극대화함.

## v1.1.88 (2026-05-12)
- [UI] **매뉴얼 시인성 극대화**: 사이드바 하단의 명령어 안내 텍스트 폰트 크기를 원본 대비 122% 수준인 `0.8rem`으로 추가 확대하여 가독성을 극대화함.

## v1.1.87 (2026-05-12)
- [UI] **매뉴얼 시인성 개선**: 사이드바 하단의 명령어 안내 텍스트가 너무 작다는 피드백을 반영하여 폰트 크기를 기존 대비 111% 확대(0.72rem)함.

## v1.1.86 (2026-05-12)
- [UI] **사이드바 매뉴얼 추가**: 서버 실행(`streamlit run`), 프로세스 확인(`tasklist`), 포트 체크(`netstat`) 명령어를 사이드바 하단에 상시 표시하여 운영 편의성 향상.

## v1.1.85 (2026-05-12)
- [UI/Logic] **양방향 매매 고정**: 사용자가 항상 양방향 포지션을 유지하므로, 사이드바의 롱/숏 허용 토글을 삭제하고 시스템적으로 항상 둘 다 허용(True)되도록 고정함.

## v1.1.84 (2026-05-12)
- [UI] **사이드바 정리**: 사이드바 하단에 중복으로 표시되던 레버리지, 증거금 등 설정 정보 텍스트 블록을 삭제하여 화면을 간소화함.

## v1.1.83 (2026-05-12)
- [Fix] **실행 에러 수정**: `st.set_page_config()`가 첫 번째 명령어가 아니어서 발생하던 `StreamlitSetPageConfigMustBeFirstCommandError`를 해결하기 위해 호출 위치를 최상단으로 조정.
- [Stability] **코드 구조 안정화**: 환경 설정 로드(`load_dotenv`) 및 중복 실행 방지 로직의 순서를 재배치하여 안정적인 부팅 보장.

## v1.1.81 (2026-05-12)
- [System] **자동화 흐름 개선**: OKX 연결 시 자동매매 ON, 스캐너 가동, 백테스트 실행이 탭 전환 없이도 즉시 수행되도록 글로벌 로직으로 이동.
- [Fix] **서버 락 충돌 해결**: 스트림릿 리로드 시 Mutex가 중복 생성되어 서버가 멈추던 현상을 `cache_resource` 적용으로 해결.
- [Maintenance] **UTC 경고 수정**: `datetime.utcnow()`를 최신 표준인 `datetime.now(timezone.utc)`로 교체하여 시스템 안정성 확보.

## v1.1.80 (2026-05-12)
- [System] **서버 중복 실행 방지**: Windows Mutex를 활용하여 동일한 PC에서 `streamlit run`이 중복으로 실행되지 않도록 차단함.
- [UI] **중복 실행 경고**: 이미 서버가 실행 중인 경우 사용자에게 안내 메시지를 표시하고 프로세스를 즉시 중단하여 이중 주문 리스크를 제거함.

## v1.1.79 (2026-05-12)
- [System] **엔진 싱글톤 패턴 적용**: `QuantumEngine`을 싱글톤으로 변경하여 여러 브라우저 탭에서 동일한 엔진 인스턴스를 공유하도록 함.
- [UI] **전역 상태 동기화**: 스트림릿의 `cache_resource`를 사용하여 세션 간 엔진 상태를 공유하고, UI 상태(API 연결, 자동매매 상태 등)가 엔진의 실제 상태와 동기화되도록 개선.
- [Stability] **이중 주문 방지**: 여러 탭을 열어도 시장 감시 및 주문 로직이 중복 실행되지 않도록 구조적 개선 완료.

## v1.1.78 (2026-05-12)
- [Git] **태그 동기화 테스트**: Git 태그 및 커밋 프로세스 점검.

## v1.1.77 (2026-05-12)
- [UI] **버전 표시 동기화**: 사이드바 로고에 표시되는 버전 텍스트를 실제 프로젝트 버전(v1.1.77)과 일치하도록 업데이트.

## v1.1.76 (2026-05-12)
- [UI] **불필요한 텍스트 제거**: 하단 리스크 및 통계 섹션의 "RISK & PERFORMANCE METRICS" 타이틀 문구를 제거하여 화면을 더 깔끔하게 정리함.

## v1.1.75 (2026-05-12)
- [Maintenance] **거래소 데이터 동기화 완료**: 
    - OKX 거래소의 과거 체결 이력을 정밀 스캔하여 `stats.json` 데이터를 실제 계정 상태와 동기화함.
    - 결과: **17승 / 13패 (승률 56.7%)**로 누적 데이터 갱신 완료.
